# for-all_snapshot_hello-world_full-stack_phoenix_elixir_defaults_sqlite3
For All snapshot Hello World full-stack web app using the Phoenix framework, the Elixir language, with defaults, and the SQLite database
